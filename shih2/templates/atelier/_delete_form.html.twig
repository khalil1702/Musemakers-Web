<form method="post" action="{{ path('app_atelier_delete', {'idAtelier': atelier.idAtelier}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ atelier.idAtelier) }}">
    <button class="btn btn-danger" type="submit">Delete</button>
</form>
