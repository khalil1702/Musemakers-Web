{{ form_start(form, {'attr': {'class': 'form'}}) }}

    <div class="form-group">
        {{ form_label(form.datedebutAtelier) }}
        {{ form_widget(form.datedebutAtelier, {'attr': {'class': 'form-control'}}) }}
        {% if not form.datedebutAtelier.vars.valid %}
            <div style="color: red; font-size: 14px;">
                {% for error in form.datedebutAtelier.vars.errors %}
                    {{ error.message }}
                {% endfor %}
            </div>
        {% endif %}
    </div>

    <div class="form-group">
        {{ form_label(form.datefinAtelier) }}
        {{ form_widget(form.datefinAtelier, {'attr': {'class': 'form-control'}}) }}
        {% if not form.datefinAtelier.vars.valid %}
            <div style="color: red; font-size: 25px;">
                {% for error in form.datefinAtelier.vars.errors %}
                    {{ error.message }}
                {% endfor %}
            </div>
        {% endif %}
    </div>

    <div class="form-group">
        {{ form_label(form.lienAtelier) }}
        {{ form_widget(form.lienAtelier, {'attr': {'class': 'form-control'}}) }}
    </div>

    <button class="btn btn-primary">{{ button_label|default('Save') }}</button>

{{ form_end(form) }}
