{% extends 'base2.html.twig' %}


{% block statoeuvre %}

<div class="d-flex justify-content-end mb-3">
        <a href="{{ path('app_oeuvre_index') }}" class="btn btn-block btn-lg btn-gradient-primary">RETOUR</a>
    </div>

<h2>Statistiques des avis</h2>


<div style="width: 300px; height: 300px;">
    <canvas id="avisChart"></canvas>
</div>

<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script>
    var positifCount = {{ positifCount }};
    var neutreCount = {{ neutreCount }};
    var negatifCount = {{ negatifCount }};

    var ctx = document.getElementById('avisChart').getContext('2d');
    var myChart = new Chart(ctx, {
        type: 'pie',
        data: {
            labels: ['Positif', 'Neutre', 'Négatif'],
            datasets: [{
                data: [positifCount, neutreCount, negatifCount],
                backgroundColor: ['#36A2EB', '#FFCE56', '#FF6384']
            }]
        },
        options: {
            // Options de configuration du graphique
        }
    });
</script>

{% endblock %}
