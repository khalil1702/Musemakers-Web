<form method="post" action="{{ path('app_exposition_delete', {'idExposition': exposition.idExposition}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ exposition.idExposition) }}">
    <button type="submit" class="btn btn-danger"><i class="fas fa-trash"></i></button>
</form>
